# 🚀 Quick Start Guide

## Setup (One Time Only)

### 1. Install Dependencies

**Backend:**
```bash
cd backend
npm install
```

**Frontend:**
```bash
cd frontend
npm install
```

## Running the App

### Step 1: Start Backend Server

```bash
cd backend
npm start
```

You'll see your network IP displayed:
```
🚀 Server running on:
   Local:   http://localhost:5000
   Network: http://192.168.1.100:5000
```

✅ Keep this terminal window open!

### Step 2: Start Frontend App

Open a NEW terminal window:

```bash
cd frontend
npm start
```

Then:
- Press `a` for Android emulator
- Press `i` for iOS simulator
- Scan QR code with Expo Go app on your phone

## ✨ That's It!

Your app will automatically connect to the backend using the network IP. No configuration needed!

## 📱 Testing on Physical Device

1. Make sure your phone and computer are on the SAME WiFi network
2. Open Expo Go app on your phone
3. Scan the QR code from the terminal
4. The app will automatically connect to your backend

## 🔧 Troubleshooting

**Can't connect to backend?**
- Check both devices are on the same WiFi
- Check firewall isn't blocking port 5000
- Restart both backend and frontend

**Need more help?**
- See [NETWORK_SETUP.md](./NETWORK_SETUP.md) for detailed troubleshooting

## 🎯 Available Scripts

### Backend
- `npm start` - Start server with network info
- `npm run dev` - Start with auto-reload (nodemon)
- `npm run server` - Start server directly

### Frontend
- `npm start` - Start Expo dev server
- `npm run android` - Run on Android
- `npm run ios` - Run on iOS
- `npm run web` - Run in web browser


accounts
user
email: anoosroschelmae@gmail.com
password: Ros123anoos

admin
roschelanoos@gmail.com
admin321
Ros123anoos